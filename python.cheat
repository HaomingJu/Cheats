
% python

# 配置pip全局源
sudo pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

# 安装python3.8
sudo add-apt-repository ppa:deadsnakes/ppa && sudo apt update && sudo apt install python3.8 python3.8-dev python3.8-venv

